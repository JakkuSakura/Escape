#if !defined(READ_MAP_H)
#define READ_MAP_H
#include "components.h"
#include <string>
#include <vector>
namespace Escape
{
class ReadMap
{
private:
    std::vector<>
public:
    ReadMap(const std::string &filaname);
    ~ReadMap();
};


} // namespace Escape


#endif // READ_MAP_H
